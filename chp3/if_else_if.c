#include <stdio.h>

int main()
{
    int num;

    printf("Enter the number\n");
    scanf("%d", &num);

    if (num == 1)
    {
        printf("Your no. is %d", num);
    }
    else if (num == 2)
    {
        printf("Your no. is %d", num);
    }
    else if (num == 3)
    {
        printf("Your no. is %d", num);
    }

    else
    {
        /* code */
        printf("out of the limit");
    }

    return 0;
}